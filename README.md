# FullStack-WebApp-Task

# Backend
# Create database named  `ecommerce`
# Run Command `npm install`
# Run Command `npx sequelize-cli db:migrate`
# Run Command `nodemon`

# Test Case
# Run Command `npm test`


# Frontend
# Run Command `npm install`
# Run Command `ng serve`